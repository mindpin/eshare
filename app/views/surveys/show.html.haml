- content_for :header do
  %h1 填写调查表：#{@survey.title}

- template = @survey.survey_template

- if template.present?
  .page-survey-show
    .template
      %span 调查表模板：
      %span= template.name

    - if !@survey.has_completed_by?(current_user) || params[:refill]
      = render_cell :survey, :form, :survey => @survey, :survey_result => @survey_result, :user => current_user
    - else
      = render_cell :survey, :result, :survey => @survey, :user => current_user

- else
  .error 调查表模板错误