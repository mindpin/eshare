%label 作业的标题
.title= @homework.title
%label 作业的内容
.content= @homework.content
%label 作业的要求
.requirements.list
  - @homework.homework_requirements.each do |requirement|
    .item= requirement.content
%label 作业的附件
.attaches.list
  - @homework.homework_attaches.each do |attach|
    = link_to attach.name, "#"
    // = attach.file_entity.attach.path

%babel 做作业的人
.answers
  - @homework.homework_records.each do |record|
    = record.creator.name