- content_for :header do
  %h1= t('page.questions.index')
  = page_button_group :questions do |g|
    - g.add '发起提问', '/questions/new', :success

.page-questions-index
  .mainp66
    .cpart
      %h3 最新动态
      = paginate @feeds
      = render_cell :timeline, :render_feeds, :user => current_user, :feeds => @feeds